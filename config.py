#SMTP Credentials for email verification. Replace the below credentials with the valid one for SMTP.
smtp_email = "SENDER_EMAIL_ADDRESS"
smtp_password = "SENDER_APP_PASSWORD"
smtp_server = "SENDER_SERVER_NAME"
smtp_port = 465     #This is Google's default port


# Twilio credentials. Replace the below credentials with the valid SID, TOKEN & Phone Number.
ACCOUNT_SID = ""
AUTH_TOKEN = ""
TWILIO_PHONE_NUMBER = ""